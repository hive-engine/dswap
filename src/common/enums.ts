export enum Chain {
    Hive = 1,
    Steem = 2
}

export enum OrderStrategy {
    TopOfTheBook = 1,
    WallNestling = 2
}

export enum SwapStatus {
    Init = 1,
    InProgress = 2,
    Success = 3,
    Failure = 4
}
